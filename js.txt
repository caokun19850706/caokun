JSON.stringify	转换为json字符串



JSON.parse	json转为数组



str.replace(/a/g, 'o');	字符替换


this.replay_data.replace(/\ +/g,"").replace(/[\r\n]/g,"") 替换空格和回车



string.substr(start,length)
	

[1, 4, -5, 10].find((n) => n < 0)	数组中查找某值，返回第一个值或undefined



arr.includes('c')	数组中查找某值，true/false



var a = {}	组装带键名的数组时要用object




item.hasOwnProperty('suber')   是否存在某键